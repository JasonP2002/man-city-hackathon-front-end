import React from "react";

export interface IAvailableProps {}

const Available: React.FunctionComponent<IAvailableProps> = (props) => {
  return (
    <div>
        
    </div>
  );
};
export default Available;
